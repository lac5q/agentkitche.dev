---
phase: 24-security-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/lib/api-client.ts
  - src/components/memroos/audit-log-panel.tsx
  - src/components/memroos/__tests__/audit-log-panel.test.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - SEC-03

must_haves:
  truths:
    - "Memroos Floor page renders an AuditLogPanel section below AgentPeersPanel"
    - "AuditLogPanel shows up to 20 audit log entries with actor, action chip, target, and relative timestamp"
    - "AuditLogPanel shows an empty state message when no entries exist"
    - "AuditLogPanel shows a loading spinner while data is being fetched"
    - "Severity chips are color-coded: info=slate, medium=amber, high=rose"
  artifacts:
    - path: "src/components/memroos/audit-log-panel.tsx"
      provides: "AuditLogPanel React component wired to useAuditLog hook"
      exports: ["AuditLogPanel"]
    - path: "src/lib/api-client.ts"
      provides: "useAuditLog(limit?) hook appended to existing hooks"
      exports: ["useAuditLog"]
    - path: "src/app/page.tsx"
      provides: "AuditLogPanel rendered below AgentPeersPanel on Memroos Floor page"
      contains: "<AuditLogPanel />"
  key_links:
    - from: "src/components/memroos/audit-log-panel.tsx"
      to: "src/lib/api-client.ts useAuditLog()"
      via: "import useAuditLog; called at component top with default limit=20"
      pattern: "useAuditLog"
    - from: "src/lib/api-client.ts useAuditLog()"
      to: "GET /api/audit-log"
      via: "fetchJSON with limit query param; refetchInterval: POLL_INTERVALS.hive"
      pattern: "fetchJSON.*api/audit-log"
    - from: "src/app/page.tsx"
      to: "src/components/memroos/audit-log-panel.tsx"
      via: "import AuditLogPanel; rendered below AgentPeersPanel"
      pattern: "<AuditLogPanel"
---

<objective>
Build the AuditLogPanel UI component and wire it into the Memroos Floor page.
The panel displays the last 20 audit log entries with actor, action, target, severity
chip, and relative timestamp, polling every 5 seconds via useAuditLog().

Purpose: SEC-03 — the dashboard exposes the audit trail so operators see all significant
agent actions in real time alongside the hive feed.

Output:
- src/lib/api-client.ts — useAuditLog() hook appended (mirrors useHiveFeed pattern)
- src/components/memroos/audit-log-panel.tsx — new component (mirrors HiveFeed pattern)
- src/components/memroos/__tests__/audit-log-panel.test.tsx — component tests
- src/app/page.tsx — AuditLogPanel imported and rendered below AgentPeersPanel
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-security-audit/24-RESEARCH.md

@src/components/memroos/hive-feed.tsx
@src/lib/api-client.ts
@src/app/page.tsx

<interfaces>
<!-- Key contracts the executor will implement against. -->

From src/components/memroos/hive-feed.tsx (mirror this pattern exactly):

formatRelativeTime(iso: string): string — copy this helper verbatim into audit-log-panel.tsx:
  diffMs less than 60s returns "just now"
  diffMs less than 60m returns "${diffMin}m ago"
  diffMs less than 24h returns "${diffHr}h ago"
  otherwise returns "${diffDay}d ago"

Component structure template (adapt column names for audit entries):
  "use client"
  wrapper div: rounded-lg border border-slate-800 bg-slate-900/50 p-4
  section header: text-xs font-medium uppercase tracking-wide text-amber-500 + h-px flex-1 bg-amber-900/40 divider
  loading state: h-6 w-6 animate-spin rounded-full border-2 border-amber-500 border-t-transparent
  empty state: py-10 text-center text-sm text-slate-500
  list: ul space-y-2 > li flex items-start gap-3 rounded-md px-2 py-1.5 hover:bg-slate-800/40

Severity color map for AuditLogPanel (differs from HiveFeed's ACTION_COLORS):
  info:   text-slate-300  bg-slate-500/15  border-slate-500/30
  medium: text-amber-300  bg-amber-500/15  border-amber-500/30
  high:   text-rose-300   bg-rose-500/15   border-rose-500/30

From src/lib/api-client.ts useHiveFeed pattern (append useAuditLog after this):
  export function useHiveFeed(limit = 20) {
    return useQuery({
      queryKey: ['hive-feed'],
      queryFn: () => fetchJSON of { actions: Array; timestamp: string } at /api/hive?limit=N,
      refetchInterval: POLL_INTERVALS.hive,
    });
  }

useAuditLog response shape from GET /api/audit-log:
  { entries: Array<{ id: number; actor: string; action: string; target: string; detail: string | null; severity: string; timestamp: string }>; timestamp: string }

From src/app/page.tsx (current render order):
  HiveFeed component
  AgentPeersPanel component
  -- AuditLogPanel goes after AgentPeersPanel --
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useAuditLog hook, AuditLogPanel component, and component tests</name>
  <files>
    src/lib/api-client.ts,
    src/components/memroos/audit-log-panel.tsx,
    src/components/memroos/__tests__/audit-log-panel.test.tsx
  </files>
  <behavior>
    - AuditLogPanel renders loading spinner while isLoading is true
    - AuditLogPanel renders "No audit events yet." empty state when entries is empty array
    - AuditLogPanel renders one list item per entry when entries are present
    - Each list item shows: actor (truncated), action chip with severity color, target, relative timestamp
    - Action chip for severity='info' has slate color classes
    - Action chip for severity='medium' has amber color classes
    - Action chip for severity='high' has rose color classes
    - Section header shows text "Audit Log"
    - useAuditLog() calls fetchJSON with /api/audit-log?limit=20 by default
    - useAuditLog(5) calls fetchJSON with /api/audit-log?limit=5
    - useAuditLog() has refetchInterval matching POLL_INTERVALS.hive (5000ms)
  </behavior>
  <action>
TDD order: write tests first (RED), then implement (GREEN).

Step 1 — Write test file src/components/memroos/__tests__/audit-log-panel.test.tsx:

Use React Testing Library and vitest. Mock useAuditLog from '@/lib/api-client' at module level.
Test cases:
- Mock isLoading=true, data=undefined — expect spinner element to be in document
- Mock isLoading=false, data={ entries: [], timestamp: '' } — expect 'No audit events yet.' text
- Mock isLoading=false, data with 3 entries of varying severity — expect 3 list items;
  verify 'info' entry has slate color class, 'medium' entry has amber color class, 'high' entry has rose color class
- Verify section heading text "Audit Log" is present

Run: npx vitest run src/components/memroos/__tests__/audit-log-panel.test.tsx — must be RED.

Step 2 — Append useAuditLog to src/lib/api-client.ts (after useHiveFeed):

export function useAuditLog(limit = 20) {
  return useQuery({
    queryKey: ['audit-log', limit],
    queryFn: () =>
      fetchJSON of type { entries: Array<{ id: number; actor: string; action: string; target: string; detail: string | null; severity: string; timestamp: string }>; timestamp: string }
      calling `/api/audit-log?limit=${limit}`,
    refetchInterval: POLL_INTERVALS.hive,
  });
}

Step 3 — Create src/components/memroos/audit-log-panel.tsx:

Place "use client" directive at top.
Import useAuditLog from '@/lib/api-client'.

Copy formatRelativeTime helper verbatim from hive-feed.tsx (same implementation, same edge cases).

SEVERITY_COLORS map:
  info:   { text: 'text-slate-300', bg: 'bg-slate-500/15', border: 'border-slate-500/30' }
  medium: { text: 'text-amber-300', bg: 'bg-amber-500/15', border: 'border-amber-500/30' }
  high:   { text: 'text-rose-300',  bg: 'bg-rose-500/15',  border: 'border-rose-500/30' }
DEFAULT_COLOR: slate (same as info — fallback for unexpected severity values)

export function AuditLogPanel({ limit = 20 }: { limit?: number }) {
  const { data, isLoading } = useAuditLog(limit);
  const entries = data?.entries ?? [];

  Return JSX structure mirroring HiveFeed exactly:
  - Outer wrapper: rounded-lg border border-slate-800 bg-slate-900/50 p-4
  - Section header: text-xs font-medium uppercase tracking-wide text-amber-500 with "Audit Log" label, plus h-px flex-1 bg-amber-900/40 divider
  - Loading state (when isLoading): amber spinner matching HiveFeed pattern
  - Empty state (when not loading and entries empty): "No audit events yet." in slate
  - List (when not loading and entries present):
      ul space-y-2
        for each entry (key=entry.id):
          li flex items-start gap-3 rounded-md px-2 py-1.5 hover:bg-slate-800/40
            span w-24 shrink-0 truncate text-xs font-medium text-slate-300: entry.actor
            span chip using SEVERITY_COLORS[entry.severity] or DEFAULT_COLOR: entry.action
            span min-w-0 flex-1 truncate text-xs text-slate-200: entry.target
            span shrink-0 tabular-nums text-xs text-slate-500: formatRelativeTime(entry.timestamp)
}

All string values rendered as React text nodes — React auto-escapes these; no raw HTML rendering needed.

Run tests — must turn GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/components/memroos/__tests__/audit-log-panel.test.tsx</automated>
  </verify>
  <done>
    AuditLogPanel renders correctly for loading, empty, and list states.
    Severity chips apply correct color classes. useAuditLog hook appended to api-client.ts.
    All component tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire AuditLogPanel into Memroos Floor page</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Modify src/app/page.tsx:

1. Add import at top of file after the existing AgentPeersPanel import line:
   import { AuditLogPanel } from "@/components/memroos/audit-log-panel";

2. In the JSX return block, add the AuditLogPanel element immediately after the AgentPeersPanel element:
   existing: &lt;AgentPeersPanel /&gt;
   add below: &lt;AuditLogPanel /&gt;

The component takes no required props (uses default limit=20 internally).
No other changes to page.tsx are needed — do not alter any other imports, state, or JSX.

After the edit, run TypeScript check to confirm no errors:
  npx tsc --noEmit
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/memroos/__tests__/audit-log-panel.test.tsx</automated>
  </verify>
  <done>
    src/app/page.tsx imports AuditLogPanel and renders it below AgentPeersPanel.
    TypeScript compilation succeeds with no errors.
    All audit log panel component tests still pass after wiring.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser→GET /api/audit-log via useAuditLog | Read-only fetch; single-user local tool; no new trust boundary beyond what Plan 01 established |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Information Disclosure | AuditLogPanel renders audit entries including action names and targets | accept | Single-user local tool behind Cloudflare tunnel; data already accessible via GET /api/audit-log from Plan 01; no new exposure |
| T-24-08 | Tampering | AuditLogPanel renders entry.actor, entry.action, entry.target as JSX text nodes | accept | React auto-escapes all string values rendered as JSX text nodes; no raw HTML injection is possible; no untrusted markup is rendered directly |
</threat_model>

<verification>
Full UI verification:

```bash
# Component tests
npx vitest run src/components/memroos/__tests__/audit-log-panel.test.tsx

# TypeScript check
npx tsc --noEmit

# Full suite (5 pre-existing failures expected)
npx vitest run
```

Visual verification (after npm run build and npm start):
- Navigate to http://localhost:3002 (Memroos Floor page)
- Scroll past AgentPeersPanel — AuditLogPanel should appear with "Audit Log" header in amber
- If no audit events exist, empty state "No audit events yet." is shown in slate
- After any hive POST action, the panel populates within 5 seconds (polling interval)
- High-severity entries show rose-colored action chips
- Medium-severity entries show amber-colored action chips
- Info-severity entries show slate-colored action chips
</verification>

<success_criteria>
- src/lib/api-client.ts exports useAuditLog() with correct response type and refetchInterval set to POLL_INTERVALS.hive
- src/components/memroos/audit-log-panel.tsx renders loading/empty/list states correctly with correct severity chip colors
- src/app/page.tsx renders AuditLogPanel below AgentPeersPanel
- TypeScript compilation succeeds with no errors
- All component tests pass; no regressions in full vitest run beyond the 5 known pre-existing failures
</success_criteria>

<output>
After completion, create .planning/phases/24-security-audit/24-02-SUMMARY.md
</output>
